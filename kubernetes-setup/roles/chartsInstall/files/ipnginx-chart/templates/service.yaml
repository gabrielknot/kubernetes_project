apiVersion: v1
kind: Service
metadata:
  name: {{ include "ipnginx-chart.fullname" . }}
  labels:
    {{- include "ipnginx-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ipnginx-chart.selectorLabels" . | nindent 4 }}
