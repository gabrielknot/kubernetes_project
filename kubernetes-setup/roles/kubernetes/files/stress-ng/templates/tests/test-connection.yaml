apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stress-ng.fullname" . }}-test-connection"
  labels:
    {{- include "stress-ng.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stress-ng.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
