apiVersion: v1
kind: Service
metadata:
  name: {{ include "stress-ng.fullname" . }}
  labels:
    {{- include "stress-ng.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stress-ng.selectorLabels" . | nindent 4 }}
